<UserControl
    x:Class="MTCSTLKiosk.Controls.Conversation"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MTCSTLKiosk.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>

        <Grid x:Name="gridCaptureBottomRight" Background="Transparent">
            <CaptureElement x:Name="captureControl" Margin="20" Stretch="UniformToFill" IsHitTestVisible="False"/>
            <Border VerticalAlignment="Stretch"  Background="#CCFFB900" Margin="20">
                <StackPanel x:Name="textStack">
                    
                </StackPanel>
                <!--<TextBlock x:Name="textTranscript" HorizontalAlignment="Stretch" FontWeight="Bold" Margin="5" Text="" TextWrapping="Wrap" FontSize="24" TextAlignment="Left" Foreground="White" />-->
            </Border>
        </Grid>
    </Grid>
</UserControl>
